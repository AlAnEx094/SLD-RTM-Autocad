# TASK: QA-ASSISTANT (MVP-0.1)

Цель: покрыть критический контракт `Kr` и минимальный сценарий расчёта щита тестами pytest.

## Deliverables

- `tests/test_kr_resolver.py`
  - тесты clamp:
    - `ki < 0.10` -> lookup как `0.10`
    - `ki > 0.80` -> lookup как `0.80`
  - тест округления вверх по `ne`:
    - например `ne=3` использует строку `ne_tab=4`
  - тесты интерполяции по `Ki` в строке `ne_tab`
- `tests/test_rtm_smoke.py`
  - smoke-тест: создать временную БД, засеять `kr_table`, создать 1 щит и 1–2 строки ввода, выполнить расчёт,
    проверить, что результаты появились и имеют ожидаемый порядок величин/точные значения для простого случая

## Acceptance criteria

- `pytest -q` проходит локально.
- Тесты изолированы (используют временную SQLite, не зависят от внешнего состояния).

